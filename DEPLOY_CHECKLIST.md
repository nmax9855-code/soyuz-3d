# 📋 Чек-лист подготовки к деплою

## ✅ Подготовка завершена

### Установленные пакеты
- ✅ vite (сборщик)
- ✅ terser (минификация JS)
- ✅ serve (локальный сервер)

### Созданные файлы конфигурации
- ✅ `vercel.json` - конфиг для Vercel
- ✅ `netlify.toml` - конфиг для Netlify
- ✅ `_redirects` - редиректы для SPA
- ✅ `.env.example` - пример переменных окружения
- ✅ `vite.config.js` - оптимизирован

### Настроенные npm скрипты
```bash
npm run dev          # Разработка (localhost:3000)
npm run build        # Сборка для продакшена
npm run preview      # Просмотр собранного проекта
npm run serve        # Запуск serve на port 3000
npm start            # build + serve
```

## 🚀 Быстрый старт для деплоя

### Локальное тестирование
```bash
npm start
```
Откроется приложение на http://localhost:3000

### Верификация перед деплоем
```bash
npm run build
# Проверить что dist/ папка создана
# Проверить что нет ошибок в консоли
```

## 🌐 Деплой на платформу (выбрать один)

### Vercel (РЕКОМЕНДУЕТСЯ - самый простой)
```bash
npm install -g vercel
vercel
# Следовать инструкциям в терминале
# или подключить GitHub репо в vercel.com/dashboard
```

### Netlify
```bash
npm install -g netlify-cli
netlify deploy --prod
# или подключить GitHub репо в netlify.com
```

### GitHub Pages
```bash
git push origin main
# Включить Pages в Settings → Pages → Deploy from branch
```

## 📊 Размер бандла (результат последней сборки)
```
dist/pages/cookie-policy.html        10.34 kB (gzip: 3.74 kB)
dist/pages/3d-modeling.html          10.52 kB (gzip: 3.59 kB)
dist/pages/3d-printing.html          13.42 kB (gzip: 4.37 kB)
dist/index.html                      14.79 kB (gzip: 4.43 kB)
dist/pages/reverse-engineering.html  15.02 kB (gzip: 4.79 kB)
dist/assets/css/styles.css           10.58 kB (gzip: 2.42 kB)
dist/assets/js/main.js                5.63 kB (gzip: 1.72 kB)
```

✅ Размеры отличные! Проект оптимизирован.

## 🔒 Примечания безопасности

1. **НИКОГДА** не коммитить `.env` (только `.env.example`)
2. Все чувствительные данные добавлять в переменные окружения платформы
3. HTTPS включен автоматически на всех платформах

## ❓ Если что-то не работает

1. Проверить что `npm run build` работает без ошибок
2. Проверить что `npm start` запускает сервер
3. Перечитать [DEPLOYMENT_GUIDE.md](DEPLOYMENT_GUIDE.md)
4. Проверить логи на платформе деплоя (Vercel/Netlify console)

---

**Готово к деплою! 🎉**
